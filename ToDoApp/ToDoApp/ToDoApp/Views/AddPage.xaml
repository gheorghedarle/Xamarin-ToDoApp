<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:templateSelector="clr-namespace:ToDoApp.Helpers.TemplateSelector"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="ToDoApp.Views.AddPage"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.Resources>
        <ResourceDictionary>
            <templateSelector:AddItemTemplateSelector x:Key="AddItemTemplateSelector" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid
            RowDefinitions="Auto, Auto, *, Auto"
            Padding="20, 0, 20, 0">
            <Label
                Grid.Row="0"
                Margin="0, 20, 0, 10"
                Style="{StaticResource Headline4_BarlowBold}"
                Text="{Binding Type, StringFormat='Add new {0}'}"
                TextColor="#333"/>
            <Grid
                Grid.Row="1"
                ColumnDefinitions="*, *">
                <Frame
                    Grid.Column="0"
                    CornerRadius="16"
                    Padding="0, 10, 0, 10"
                    Margin="0, 0, 2, 0">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer 
                            Command="{Binding ChangeTypeCommand}"
                            CommandParameter="task"></TapGestureRecognizer>
                    </Frame.GestureRecognizers>
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup>
                            <VisualState x:Name="selected">
                                <VisualState.StateTriggers>
                                    <StateTrigger IsActive="{Binding Type, Converter={xct:EqualConverter}, ConverterParameter='task'}"></StateTrigger>
                                </VisualState.StateTriggers>
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="White"/>
                                    <Setter Property="HasShadow" Value="True"/>
                                    <Setter Property="BorderColor" Value="White"/>
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="notSelected">
                                <VisualState.StateTriggers>
                                    <StateTrigger IsActive="{Binding Type, Converter={xct:EqualConverter}, ConverterParameter='list'}"></StateTrigger>
                                </VisualState.StateTriggers>
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="#F1F0F7"/>
                                    <Setter Property="HasShadow" Value="False"/>
                                    <Setter Property="BorderColor" Value="{AppThemeBinding Light={StaticResource LightCardBorderColor}, Dark={StaticResource DarkCardBorderColor}}"/>
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                    <Label
                        Text="Task"
                        FontSize="{StaticResource Body1FontSize}"
                        FontFamily="Barlow_SemiBold"
                        HorizontalTextAlignment="Center">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup>
                                <VisualState x:Name="selected">
                                    <VisualState.StateTriggers>
                                        <StateTrigger IsActive="{Binding Type, Converter={xct:EqualConverter}, ConverterParameter='task'}"></StateTrigger>
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Property="TextColor" Value="#0E1837"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="notSelected">
                                    <VisualState.StateTriggers>
                                        <StateTrigger IsActive="{Binding Type, Converter={xct:EqualConverter}, ConverterParameter='list'}"></StateTrigger>
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Property="TextColor" Value="#A6A3BD"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Label>
                </Frame>
                <Frame
                    Grid.Column="1"
                    CornerRadius="16"
                    Padding="0, 10, 0, 10"
                    Margin="2, 0, 0, 0">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer 
                            Command="{Binding ChangeTypeCommand}"
                            CommandParameter="list"></TapGestureRecognizer>
                    </Frame.GestureRecognizers>
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup>
                            <VisualState x:Name="selected">
                                <VisualState.StateTriggers>
                                    <StateTrigger IsActive="{Binding Type, Converter={xct:EqualConverter}, ConverterParameter='list'}"></StateTrigger>
                                </VisualState.StateTriggers>
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="White"/>
                                    <Setter Property="HasShadow" Value="True"/>
                                    <Setter Property="BorderColor" Value="White"/>
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="notSelected">
                                <VisualState.StateTriggers>
                                    <StateTrigger IsActive="{Binding Type, Converter={xct:EqualConverter}, ConverterParameter='task'}"></StateTrigger>
                                </VisualState.StateTriggers>
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="#F1F0F7"/>
                                    <Setter Property="HasShadow" Value="False"/>
                                    <Setter Property="BorderColor" Value="{AppThemeBinding Light={StaticResource LightCardBorderColor}, Dark={StaticResource DarkCardBorderColor}}"/>
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                    <Label
                        Text="List"
                        FontSize="{StaticResource Body1FontSize}"
                        FontFamily="Barlow_SemiBold"
                        HorizontalTextAlignment="Center">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup>
                                <VisualState x:Name="selected">
                                    <VisualState.StateTriggers>
                                        <StateTrigger IsActive="{Binding Type, Converter={xct:EqualConverter}, ConverterParameter='list'}"></StateTrigger>
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Property="TextColor" Value="#0E1837"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="notSelected">
                                    <VisualState.StateTriggers>
                                        <StateTrigger IsActive="{Binding Type, Converter={xct:EqualConverter}, ConverterParameter='task'}"></StateTrigger>
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Property="TextColor" Value="#A6A3BD"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Label>
                </Frame>
            </Grid>
            <CarouselView
                Grid.Row="2"
                CurrentItem="{Binding Type}"
                ItemsSource="{Binding ItemList}"
                ItemTemplate="{StaticResource AddItemTemplateSelector}"
                IsSwipeEnabled="False"></CarouselView>
            <Button
                Grid.Row="3"
                BackgroundColor="{StaticResource PrimaryColor}"
                Command="{Binding CreateCommand}"
                CornerRadius="28"
                FontSize="{StaticResource ButtonFontSize}"
                FontFamily="Barlow_Bold"
                Margin="0, 0, 0, 16"
                Padding="20, 18"
                Text="{Binding Type, StringFormat='Add new {0}'}"
                TextColor="White"
                VerticalOptions="End"></Button>
        </Grid>
    </ContentPage.Content>
</ContentPage>