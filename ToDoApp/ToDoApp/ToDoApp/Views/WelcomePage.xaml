<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:templateSelector="clr-namespace:ToDoApp.Helpers.TemplateSelector"
             x:Class="ToDoApp.Views.WelcomePage"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.Resources>
        <ResourceDictionary>
            <templateSelector:AuthTemplateSelector x:Key="AuthTemplateSelector" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid
            RowDefinitions="*, Auto">
            <Grid.Background>
                <LinearGradientBrush 
                    StartPoint="0, 0" 
                    EndPoint="0, 1">
                    <GradientStop Color="#1eae98" Offset="0.1"></GradientStop>
                    <GradientStop Color="#233e8b" Offset="0.8"></GradientStop>
                </LinearGradientBrush>
            </Grid.Background>
            <CarouselView
                Margin="0"
                CurrentItem="{Binding CurrentAuthScreen}"
                ItemsSource="{Binding AuthScreenList}"
                ItemTemplate="{StaticResource AuthTemplateSelector}"
                IsSwipeEnabled="False">
            </CarouselView>
        </Grid>
    </ContentPage.Content>
</ContentPage>